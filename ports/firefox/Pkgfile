# Description: Mozilla web browser
# Depends on: autoconf-2.13 aom cbindgen dav1d dbus-glib gtk gtk3 libnotify llvm lld clang nasm nodejs nss alsa-lib python3 startup-notification unzip yasm zip

name=firefox
version=153.3.0
release=mix-1
source=(https://archive.mozilla.org/pub/firefox/releases/${version}esr/source/firefox-${version}esr.source.tar.xz
        https://www.linuxfromscratch.org/patches/blfs/svn/firefox-$version-clang_23_rust_1_98-1.patch
        mozconfig)

build() {
  cd $name-$version

  patch -Np1 -i ../firefox-$version-clang_23_rust_1_98-1.patch

  cp -v $SRC/mozconfig .

  export CFLAGS="$CFLAGS -w"
  export CXXFLAGS="$CXXFLAGS -w"
  #export CC=clang CXX=clang++ AR=llvm-ar NM=llvm-nm RANLIB=llvm-ranlib
  export CC=gcc CXX=g++
  export MACH_BUILD_PYTHON_NATIVE_PACKAGE_SOURCE=none
  export MOZBUILD_STATE_PATH=${PWD}/mozbuild
  export MOZ_NOSPAM=1

  #./mach configure
  ./mach build
  DESTDIR=$PKG ./mach install

  install -d $PKG/usr/share/pixmaps
  ln -sv /usr/lib/firefox/browser/chrome/icons/default/default128.png $PKG/usr/share/pixmaps/firefox.png

  rm -rfv $PKG/usr/lib/firefox/browser/features
  rm -fv $PKG/usr/lib/firefox/removed-files
}
