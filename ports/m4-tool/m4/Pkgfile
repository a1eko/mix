# Description: macro processor

name=m4
version=1.4.17
release=0
source=(ftp://ftp.gnu.org/gnu/m4/m4-$version.tar.xz)

build() {
  cd $name-$version
  ./configure --prefix=/tools
  make
  make prefix=$PKG/tools install
  rm -rv $PKG/tools/share/info
}
