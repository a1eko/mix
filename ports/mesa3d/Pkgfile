# Description: Mesa 3D Graphics Library
# URL: http://www.mesa3d.org
# Maintainer: CRUX Xorg Team, xorg-ports at crux dot nu
# Depends on: expat libdrm libvdpau xorg-dri3proto xorg-glproto xorg-libxdamage xorg-libxshmfence xorg-libxvmc xorg-presentproto xorg-libxxf86vm

name=mesa3d
version=12.0.5
release=mix-1
source=(ftp://ftp.freedesktop.org/pub/mesa/$version/mesa-$version.tar.xz)

build() {
	cd mesa-$version

	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--enable-gbm \
		--enable-gles1 \
		--enable-gles2 \
		--enable-glx-tls \
		--enable-osmesa \
		--enable-texture-float \
		--enable-xa \
		--enable-vdpau \
		--disable-dependency-tracking \
		--with-egl-platforms=x11,drm \
		--with-gallium-drivers=svga,swrast

	make
	make DESTDIR=$PKG install
}
