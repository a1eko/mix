# Description: text processing language interpreter

name=gawk
version=4.1.4
release=0
source=(ftp://ftp.gnu.org/gnu/gawk/gawk-$version.tar.xz)

build() {
  cd $name-$version
  ./configure --prefix=/tools
  make
  make DESTDIR=$PKG install
  rm -rv $PKG/tools/share/{info,man}
}
