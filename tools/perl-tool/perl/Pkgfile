# Description: programming language and script interpreter

name=perl
version=5.20.1
release=0
source=(http://cpan.org/src/5.0/perl-$version.tar.bz2)

build() {
  cd $name-$version
  sh Configure -des -Dprefix=/tools -Dlibs=-lm
  make
  mkdir -pv $PKG/tools/bin
  cp -v perl cpan/podlators/pod2man $PKG/tools/bin/
  mkdir -pv $PKG/tools/lib/perl5/$version
  cp -Rv lib/* $PKG/tools/lib/perl5/$version/
  find $PKG -name "README*" -delete
}
