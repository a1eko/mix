# Description: data compression utilities

name=xz
version=5.2.2
release=0
source=(http://tukaani.org/xz/xz-$version.tar.bz2)

build() {
  cd $name-$version
  ./configure --prefix=/tools
  make
  make DESTDIR=$PKG install
  rm -rv $PKG/tools/share/doc
}
