# Description: line-oriented text editor

name=ed
version=1.10
release=mix-1
source=(ftp://ftp.gnu.org/pub/gnu/ed/ed-$version.tar.lz)

build() {
  cd $name-$version
  ./configure --prefix=/usr
  make
  make DESTDIR=$PKG install install-man
  rm -rv $PKG/usr/share/info
}
